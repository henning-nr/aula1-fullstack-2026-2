// Evento de clique no botão salvar (VANILLA)
btn = document.getElementById("btnSalvar")
btn.addEventListener('click', salvar)

// Evento de clique no botão salvar (JQUERY)
$("#btnSalvar").click(salvar)

